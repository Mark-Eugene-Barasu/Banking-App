"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;var _config = await jitiImport("prisma/config");
var _fs = await jitiImport("fs");
var _path = await jitiImport("path");

// Manually parse .env since Prisma 7 runs this config in an isolated context
function loadEnv() {
  try {
    const envPath = (0, _path.resolve)(__dirname, ".env");
    const lines = (0, _fs.readFileSync)(envPath, "utf-8").split("\n");
    for (const line of lines) {
      const trimmed = line.trim();
      if (!trimmed || trimmed.startsWith("#")) continue;
      const idx = trimmed.indexOf("=");
      if (idx === -1) continue;
      const key = trimmed.slice(0, idx).trim();
      const val = trimmed.slice(idx + 1).trim().replace(/^["']|["']$/g, "");
      if (!process.env[key]) process.env[key] = val;
    }
  } catch {}
}

loadEnv();var _default = exports.default =

(0, _config.defineConfig)({
  datasourceUrl: process.env.DATABASE_URL
}); /* v9-aadd626c17cb0aef */
